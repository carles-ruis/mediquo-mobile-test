<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--   <item name="searchViewStyle">@style/mediquoSearchViewStyle</item> -->
        <item name="autoCompleteTextViewStyle">@style/mediquoAutocompleteStyle</item>
    </style>

    <style name="appToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Primary" />

    <!-- <style name="mediquoSearchViewStyle" parent="Widget.AppCompat.Light.SearchView">
         <item name="searchHintIcon">@null</item>
         <item name="searchIcon">@drawable/ic_search_white_24dp</item>
         <item name="closeIcon">@drawable/ic_close_white_24dp</item>
     </style>-->

    <style name="mediquoAutocompleteStyle" parent="Widget.AppCompat.Light.AutoCompleteTextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

</resources>
